; Values from c[188], c[189], c[190], c[191] will be captured
#input matrix4 96
#output matrix4 188

; Just pass through the input data in v1 as the test is simply validating how
; various types are expanded.
mov #output[0], v1
