; Sets all registers to a known state, passed as the diffuse color.

mov oPos, v0
mov oD0, v3
mov oD1, v3
mov oB0, v3
mov oB1, v3
mov oFog, v3
mov oPts, v3
mov oTex0, v3
mov oTex1, v3
mov oTex2, v3
mov oTex3, v3

mov r0, v3
mov r1, v3
mov r2, v3
mov r3, v3
mov r4, v3
mov r5, v3
mov r6, v3
mov r7, v3
mov r8, v3
mov r9, v3
mov r10, v3
mov r11, v3
